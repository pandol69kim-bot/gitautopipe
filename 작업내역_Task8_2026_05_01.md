# 작업내역 - Task 8: Skill/Insight 게시물 자동 생성 시스템 (Vercel)

**날짜**: 2026-05-01  
**브랜치**: main  
**커밋**: `e0f4d6c`

---

## 개요

셀피시 클럽 AI 에이전트 협업 시스템의 Task 8을 완료했습니다.  
Skill/Insight 마크다운 문서를 정적 사이트로 빌드하고 Vercel REST API로 자동 배포하는 `WebsiteDeployer`를 구현했습니다.

---

## 구현 파일

### 신규 생성

| 파일 | 역할 |
|------|------|
| `src/types/deployer.ts` | BuildResult, DeploymentResult, DeploymentStatus, SiteCategory 타입 정의 |
| `src/workflows/website-deployer.ts` | WebsiteDeployer 클래스 구현 |
| `src/workflows/website-deployer.test.ts` | TDD 테스트 29개 |

### 수정

| 파일 | 변경 내용 |
|------|-----------|
| `src/types/index.ts` | `export * from './deployer'` 추가 |

---

## 구현 상세

### 1. 타입 정의 (`src/types/deployer.ts`)

```typescript
type SiteCategory = 'ai-tools' | 'platform' | 'insights' | 'uncategorized';
type DeploymentState = 'QUEUED' | 'BUILDING' | 'READY' | 'ERROR' | 'CANCELED';

interface SitePage {
  slug: string;
  title: string;
  category: SiteCategory;
  htmlContent: string;
  markdownSource: string;
  searchText: string;
}

interface BuildResult {
  pages: SitePage[];
  searchIndex: SearchIndexEntry[];
  outputPath: string;
  pageCount: number;
  builtAt: Date;
}

interface DeploymentResult {
  deploymentId: string;
  url: string;
  previewUrl: string;
  state: DeploymentState;
  createdAt: Date;
}

interface WebsiteDeployerConfig {
  vercelToken: string;
  projectId: string;
  teamId?: string;
  notificationWebhookUrl?: string;
}
```

### 2. WebsiteDeployer 클래스 (`src/workflows/website-deployer.ts`)

#### 주요 메서드

| 메서드 | 설명 |
|--------|------|
| `buildSite(sourceFolder)` | 마크다운 스캔 → HTML 변환 + 검색 인덱스 생성 |
| `deployToVercel(buildOutput)` | Vercel REST API POST + Bearer 인증 배포 |
| `getDeploymentStatus(deploymentId)` | 배포 상태 폴링 (QUEUED/BUILDING/READY/ERROR) |
| `rollback(deploymentId)` | 특정 배포 롤백 |
| `sendNotification(result)` | Slack/Discord webhook 알림 |
| `classifyCategory(category?, title)` _(static)_ | 카테고리 자동 분류 |
| `markdownToHtml(markdown)` _(static)_ | 마크다운 → HTML 변환 |

#### 카테고리 자동 분류 키워드

| 카테고리 | 키워드 |
|---------|--------|
| `ai-tools` | ai, gpt, llm, claude, openai, 인공지능, 에이전트, chatgpt, prompt, 프롬프트 |
| `platform` | vercel, github, docker, aws, gcp, ci/cd, deploy, 배포, actions, kubernetes |
| `insights` | 회고, 인사이트, insight, 성장, 배움, retrospect, 학습, 경험, 정리 |
| `uncategorized` | 위 키워드에 해당 없음 |

분류 우선순위: frontmatter `category` > 제목 키워드 매칭 > `uncategorized`

#### 마크다운 → HTML 변환 (`markdownToHtml`)

| 마크다운 | HTML 출력 |
|---------|-----------|
| `# 제목` | `<h1>제목</h1>` |
| `## 소제목` | `<h2>소제목</h2>` |
| `### 작은 제목` | `<h3>작은 제목</h3>` |
| `- 항목` | `<li>항목</li>` |
| `**굵게**` | `<strong>굵게</strong>` |
| `*기울임*` | `<em>기울임</em>` |
| `` `코드` `` | `<code>코드</code>` |
| `[텍스트](url)` | `<a href="url">텍스트</a>` |
| 일반 텍스트 | `<p>텍스트</p>` |

#### Vercel API 엔드포인트

```
POST https://api.vercel.com/v13/deployments          → 배포 생성
GET  https://api.vercel.com/v13/deployments/:id       → 상태 조회
POST https://api.vercel.com/v13/deployments/:id/rollback → 롤백
```

#### 설계 포인트

- `FetchFn` 타입으로 `fetch` 의존성 주입 (테스트 가능성 확보, Node 18+ 네이티브 fetch 호환)
- `gray-matter`로 frontmatter 파싱
- `notificationWebhookUrl` 설정 시에만 webhook 호출 (없으면 no-op)
- `teamId` 옵션 시 Vercel API 쿼리스트링 `?teamId=` 자동 추가

---

## TDD 결과

### 테스트 커버리지

```
Test Files: 12 passed (12)
Tests:      209 passed (209)   ← 신규 29개 포함
```

### 테스트 케이스 목록 (29개)

#### buildSite (5개)
- BuildResult 구조를 반환한다
- 마크다운 파일 수만큼 페이지를 생성한다
- 각 페이지에 slug, title, htmlContent, category가 있다
- htmlContent에 `<h1>` 태그가 포함된다
- 검색 인덱스가 페이지 수와 동일하게 생성된다

#### classifyCategory static (5개)
- frontmatter category가 있으면 해당 카테고리를 사용한다
- category 없고 제목에 AI/GPT 키워드가 있으면 ai-tools로 분류한다
- category 없고 제목에 플랫폼 키워드가 있으면 platform으로 분류한다
- category 없고 제목에 인사이트 키워드가 있으면 insights로 분류한다
- 분류 불가능하면 uncategorized를 반환한다

#### markdownToHtml static (8개)
- # 제목을 `<h1>`으로 변환한다
- ## 제목을 `<h2>`로 변환한다
- ### 제목을 `<h3>`로 변환한다
- - 목록을 `<li>`로 변환한다
- **굵은** 텍스트를 `<strong>`으로 변환한다
- \`인라인 코드\`를 `<code>`로 변환한다
- 일반 단락을 `<p>`로 감싼다
- 빈 줄은 건너뛴다

#### deployToVercel (5개)
- DeploymentResult 구조를 반환한다
- Vercel API에 POST 요청을 보낸다
- Authorization 헤더에 Bearer 토큰을 포함한다
- deploymentId가 응답에 포함된다
- Vercel API 실패 시 에러를 던진다

#### getDeploymentStatus (2개)
- DeploymentStatus 구조를 반환한다
- READY 상태이면 url이 포함된다

#### rollback (1개)
- Vercel API에 롤백 POST 요청을 보낸다

#### sendNotification (3개)
- webhookUrl이 없으면 아무것도 하지 않는다
- webhookUrl이 있으면 POST 요청을 보낸다
- 알림 payload에 배포 URL이 포함된다

---

## 커밋 정보

```
e0f4d6c feat: Implement WebsiteDeployer for Skill/Insight auto-deployment (Task 8)
```

---

## Task 진행 현황

| ID | 태스크 | 상태 |
|----|--------|------|
| 1 | 프로젝트 초기 설정 및 개발 환경 구성 | ✅ done |
| 2 | Obsidian 볼트 구조 및 폴더 스캐너 구현 | ✅ done |
| 3 | GitHub 연동 및 코드 자동화 모듈 구현 | ✅ done |
| 4 | Claude Code 분석 엔진 구현 | ✅ done |
| 5 | Analysis 결과 자동 커밋 워크플로우 구현 | ✅ done |
| 6 | LinkedIn 게시물 자동 생성 시스템 구현 | ✅ done |
| 7 | Notion MCP 연동 및 Meetings 자동화 | ✅ done |
| **8** | **Skill/Insight 게시물 자동 생성 시스템 (Vercel)** | **✅ done** |
| 9 | 자동화 워크플로우 오케스트레이터 구현 | ⏳ pending |
| 10 | CLI 인터페이스 및 스케줄러 구현 | ⏳ pending |
| 11 | AA 데이터 키트 샘플링 및 자동화 | ⏳ pending |
| 12 | 문서 및 배포 설정 시스템 구현 | ⏳ pending |
