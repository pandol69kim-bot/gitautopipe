# 프로그램 시작방법

작성일: 2026-05-01  
작업 위치: `D:\TM_PROJECT_셀피시\코드엑스개발`

## 1. 사전 준비

Node.js 18 이상이 필요합니다.

```powershell
node -v
npm -v
```

의존성이 없거나 `node_modules/`가 없는 경우 설치합니다.

```powershell
npm install
```

`.env` 파일은 이미 로컬에 존재합니다. 새 환경에서 시작할 때는 `.env.example`을 참고해 필요한 값을 채워야 합니다.

주요 환경 변수:

| 변수 | 용도 |
| --- | --- |
| `GITHUB_TOKEN` | GitHub 연동 |
| `CLAUDE_API_KEY` | Claude 분석 연동 |
| `NOTION_TOKEN` | Notion 연동 |
| `VERCEL_TOKEN` | Vercel 배포 연동 |

## 2. 빌드 후 실행

가장 안정적인 실행 방식입니다.

```powershell
npm run build
node dist\index.js
```

또는 `package.json`의 시작 스크립트를 사용할 수 있습니다.

```powershell
npm run build
npm start
```

현재 확인 결과 `node dist\index.js`는 정상 실행되며 다음 시작 메시지를 출력합니다.

```text
셀피시 클럽 AI 에이전트 시스템 시작
```

## 3. 개발 모드 실행

TypeScript 소스를 바로 실행하려면 다음 명령을 사용합니다.

```powershell
npm run dev
```

이 명령은 내부적으로 다음 파일을 실행합니다.

```powershell
ts-node src/index.ts
```

## 4. CLI 실행

빌드 후 CLI는 다음처럼 실행할 수 있습니다.

```powershell
npm run build
node dist\cli\index.js --help
```

사용 가능한 CLI 명령:

| 명령 | 설명 |
| --- | --- |
| `scan` | Obsidian 볼트 폴더 스캔 |
| `sync` | 외부 서비스 동기화 |
| `analyze` | 지정 주차 볼트 데이터 분석 |
| `deploy` | Skill/Insight 사이트 Vercel 배포 |
| `workflow <workflowId>` | 워크플로우 실행 |
| `status` | 시스템 상태 확인 |
| `interactive` 또는 `i` | 인터랙티브 모드 실행 |

상태 확인 예시:

```powershell
node dist\cli\index.js status --output json
```

현재 확인된 상태:

```json
{
  "workflows": [
    { "id": "onMissionUpdate", "registered": true, "historyCount": 0 },
    { "id": "onMeetingSync", "registered": true, "historyCount": 0 },
    { "id": "onSkillUpdate", "registered": true, "historyCount": 0 },
    { "id": "weeklyDigest", "registered": true, "historyCount": 0 }
  ],
  "schedules": 1
}
```

## 5. 자주 쓰는 점검 명령

```powershell
npm run typecheck
npm run build
npm run security:scan
```

현재 통과가 확인된 명령:

| 명령 | 결과 |
| --- | --- |
| `npm run typecheck` | 통과 |
| `npm run build` | 통과 |
| `npm run security:scan` | 통과 |

## 6. 현재 실패하는 명령

테스트:

```powershell
npm test
```

현재 실패 원인:

```text
failed to load config from vitest.config.ts
Error: spawn EPERM
```

Lint:

```powershell
npm run lint
```

현재 실패 원인:

```text
ESLint couldn't find an eslint.config.(js|mjs|cjs) file.
```

설치된 ESLint가 10.x이므로 `.eslintrc.json` 대신 flat config 형식의 `eslint.config.js`가 필요합니다.

## 7. 추천 시작 순서

처음 실행할 때는 아래 순서가 가장 무난합니다.

```powershell
cd D:\TM_PROJECT_셀피시\코드엑스개발
npm install
npm run typecheck
npm run build
node dist\cli\index.js status --output json
```

기본 엔트리만 확인하려면 다음으로 충분합니다.

```powershell
npm run build
npm start
```
