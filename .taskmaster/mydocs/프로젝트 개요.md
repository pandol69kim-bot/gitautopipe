# 프로젝트 개요

작성일: 2026-05-01  
작업 위치: `D:\TM_PROJECT_셀피시\코드엑스개발`

## 1. 한 줄 요약

셀피시 클럽 AI 에이전트 협업 시스템은 Obsidian/Markdown 볼트, GitHub, Claude, Notion, LinkedIn, Vercel 배포 흐름을 TypeScript 기반 CLI와 워크플로우로 자동화하는 Node.js 프로젝트입니다.

## 2. 기술 스택

| 구분 | 내용 |
| --- | --- |
| 런타임 | Node.js 18 이상 |
| 언어 | TypeScript |
| 모듈 방식 | CommonJS |
| 빌드 출력 | `dist/` |
| CLI 프레임워크 | Commander.js, Inquirer.js |
| 테스트 | Vitest |
| 주요 연동 | Claude SDK, GitHub Octokit, Notion SDK, simple-git |
| 주요 유틸 | dotenv, zod, gray-matter, chokidar |

## 3. 주요 디렉터리

| 경로 | 역할 |
| --- | --- |
| `src/index.ts` | 기본 프로그램 엔트리포인트 |
| `src/cli/` | CLI 명령, 포맷터, 인터랙티브 모드, 보안 실행 래퍼 |
| `src/core/` | 볼트 스캐너, 리포트 생성 등 핵심 로직 |
| `src/integrations/` | Claude, GitHub, LinkedIn, Notion 연동 |
| `src/workflows/` | 자동화 워크플로우 오케스트레이터와 배포 흐름 |
| `src/security/` | 시크릿 관리, 시크릿 스캔, 접근 제어, 감사 로그, rate limit |
| `src/types/` | 도메인 타입 정의 |
| `scripts/` | 보안 스캔 등 보조 스크립트 |
| `aa-starter-kit/` | AA 스타터 키트 템플릿/문서 |
| `.taskmaster/` | Taskmaster 작업 정의와 진행 메타데이터 |

## 4. 현재 구현 범위

현재 코드 기준으로 다음 영역이 구현되어 있습니다.

| 영역 | 상태 |
| --- | --- |
| 프로젝트 초기 설정 | 완료 |
| Obsidian/Markdown 볼트 스캔 기반 | 완료 |
| GitHub 동기화 기반 | 완료 |
| Claude 분석 연동 기반 | 완료 |
| Analysis 리포트 생성 | 완료 |
| LinkedIn 콘텐츠 생성 | 완료 |
| Notion Meetings 동기화 | 완료 |
| Skill/Insight 배포 흐름 | 완료 |
| 워크플로우 오케스트레이터 | 완료 |
| CLI 및 운영 대시보드 명령 | 완료 |
| AA 스타터 키트 | 완료 |
| 보안/권한 관리 | 코드 일부 존재, Taskmaster 상위 작업은 아직 `pending`으로 남아 있음 |

Taskmaster 메타데이터 기준 전체 상위 작업은 12개이며 `completedCount`는 11개입니다. 다만 `.taskmaster/tasks/tasks.json`은 현재 JSON 파싱 오류가 있어 Taskmaster 도구에서 바로 읽지 못할 가능성이 있습니다.

## 5. Git 작업 상태

2026-05-01 확인 기준:

| 항목 | 상태 |
| --- | --- |
| 현재 브랜치 | `main` |
| 원격 대비 | `origin/main`보다 7커밋 앞섬 |
| 최근 커밋 | Task 5부터 Task 11까지 구현 커밋 존재 |
| 워킹트리 | 수정/미추적 파일 다수 존재 |

주요 변경 파일에는 `src/cli`, `src/core/report-generator`, `src/integrations/linkedin`, `src/integrations/notion`, `src/workflows`, `src/security`, `src/types/security.ts`, `scripts/secret-scan.ts`, `.githooks`, `.gitignore`, `package.json`, `tsconfig.json` 등이 포함됩니다.

## 6. 검증 결과

| 명령 | 결과 | 메모 |
| --- | --- | --- |
| `npm run typecheck` | 통과 | `tsc --noEmit` 성공 |
| `npm run build` | 통과 | `tsc` 빌드 성공 |
| `node dist\index.js` | 통과 | 기본 시작 메시지 출력 |
| `node dist\cli\index.js --help` | 통과 | CLI 명령 목록 출력 |
| `node dist\cli\index.js status --output json` | 통과 | 사전 정의 워크플로우 4개 등록 확인 |
| `npm run security:scan` | 통과 | 검사 대상 파일 없음 |
| `npm test` | 실패 | Vitest 설정 로딩 중 `spawn EPERM` 발생 |
| `npm run lint` | 실패 | ESLint 10에서 `eslint.config.js` 필요 |

## 7. 현재 주의할 점

1. ESLint 설정은 `.eslintrc.json` 기반인데 설치된 ESLint 10은 flat config인 `eslint.config.js`를 요구합니다.
2. Vitest는 현재 환경에서 Vite/Vitest 설정 로딩 중 `spawn EPERM`으로 시작하지 못합니다.
3. `.taskmaster/tasks/tasks.json`은 일부 문자열 이스케이프/인코딩 문제로 `ConvertFrom-Json` 파싱이 실패했습니다.
4. 콘솔에서 일부 기존 문서와 Taskmaster 텍스트가 깨져 보입니다. 실제 파일 인코딩과 터미널 출력 인코딩을 분리해서 확인할 필요가 있습니다.
5. 현재 워킹트리에 수정/미추적 파일이 많으므로 커밋 전 포함/제외 범위를 명확히 정해야 합니다.

## 8. 다음 권장 작업

1. `eslint.config.js` 추가 또는 ESLint 버전 조정으로 lint 복구
2. Vitest `spawn EPERM` 원인 확인 및 테스트 실행 안정화
3. `.taskmaster/tasks/tasks.json` 파싱 오류 복구
4. 보안 관련 구현 파일과 Taskmaster Task 12 상태 동기화
5. 변경 파일 검토 후 커밋 범위 정리
