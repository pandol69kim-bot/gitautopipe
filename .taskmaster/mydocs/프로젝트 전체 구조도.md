# 프로젝트 전체 구조도

작성일: 2026-05-01  
프로젝트 경로: `D:\TM_PROJECT_셀피시\코드엑스개발`

## 전체 구조

```mermaid
flowchart TB
  Root["selfisclub-codex"]

  Root --> Src["src - TypeScript source"]
  Root --> Dist["dist - build output"]
  Root --> Scripts["scripts - helper scripts"]
  Root --> Starter["aa-starter-kit"]
  Root --> Taskmaster[".taskmaster"]
  Root --> Configs["config files"]
  Root --> Docs["docs"]

  Configs --> Package["package.json"]
  Configs --> TS["tsconfig.json"]
  Configs --> TSEslint["tsconfig.eslint.json"]
  Configs --> ESLint["eslint.config.js"]
  Configs --> Vitest["vitest.config.ts"]
  Configs --> Env[".env / .env.example"]

  Scripts --> SecretScan["secret-scan.ts"]
  Taskmaster --> Tasks["tasks.json"]
```

## src 모듈 구조

```mermaid
flowchart LR
  Entry["src/index.ts"]

  subgraph CLI["src/cli"]
    CLIIndex["index.ts"]
    Commands["commands.ts"]
    Interactive["interactive.ts"]
    Formatter["formatter.ts"]
    ConfigManager["config-manager.ts"]
    CLISecurity["security.ts"]
    Logger["logger.ts"]
  end

  subgraph Core["src/core"]
    Vault["vault-scanner.ts"]
    Report["report-generator.ts"]
  end

  subgraph Integrations["src/integrations"]
    Claude["claude.ts"]
    GitHub["github.ts"]
    LinkedIn["linkedin.ts"]
    Notion["notion.ts"]
    ClaudeFactory["claude-factory.ts"]
  end

  subgraph Workflows["src/workflows"]
    Orchestrator["orchestrator.ts"]
    Deployer["website-deployer.ts"]
  end

  subgraph Security["src/security"]
    Access["access-control.ts"]
    Audit["audit-logger.ts"]
    Rate["rate-limiter.ts"]
    Secrets["secret-manager.ts"]
    Scanner["secret-scanner.ts"]
  end

  subgraph Types["src/types"]
    TypeDefs["domain types"]
  end

  subgraph Utils["src/utils"]
    EnvUtil["env.ts"]
  end

  Entry --> EnvUtil
  CLIIndex --> Commands
  CLIIndex --> Interactive
  CLIIndex --> ConfigManager
  CLIIndex --> CLISecurity
  CLIIndex --> Orchestrator

  Commands --> Orchestrator
  Commands --> Formatter

  CLISecurity --> Access
  CLISecurity --> Audit
  CLISecurity --> Rate
  CLISecurity --> Secrets

  Orchestrator --> Core
  Orchestrator --> Integrations
  Deployer --> Integrations

  Core --> Types
  Integrations --> Types
  Workflows --> Types
  Security --> Types
```

## 계층 구조

```mermaid
flowchart TB
  User["사용자 / 터미널"]
  CLI["CLI 계층"]
  SecurityLayer["보안 계층"]
  WorkflowLayer["워크플로우 계층"]
  DomainLayer["도메인 계층"]
  IntegrationLayer["외부 연동 계층"]
  TypesLayer["타입/설정 계층"]

  User --> CLI
  CLI --> SecurityLayer
  SecurityLayer --> WorkflowLayer
  WorkflowLayer --> DomainLayer
  WorkflowLayer --> IntegrationLayer
  DomainLayer --> TypesLayer
  IntegrationLayer --> TypesLayer
  SecurityLayer --> TypesLayer
```

## 주요 파일 역할

| 파일/폴더 | 역할 |
| --- | --- |
| `src/index.ts` | 기본 프로그램 실행 진입점 |
| `src/cli/index.ts` | CLI 명령 등록 및 실행 진입점 |
| `src/cli/commands.ts` | `scan`, `sync`, `analyze`, `deploy`, `workflow`, `status` 처리 |
| `src/cli/security.ts` | CLI 명령 실행 전 권한, 시크릿, rate limit, 감사 로그 처리 |
| `src/workflows/orchestrator.ts` | 사전 정의 워크플로우 등록 및 실행 |
| `src/core/vault-scanner.ts` | Obsidian/Markdown 볼트 스캔 |
| `src/core/report-generator.ts` | Analysis 보고서 생성 |
| `src/integrations/*` | 외부 서비스별 연동 모듈 |
| `src/security/*` | 보안 기능 구현 |
| `src/types/*` | 모듈 간 타입 계약 |
| `scripts/secret-scan.ts` | 시크릿 노출 검사 |

## 빌드와 검증 구조

```mermaid
flowchart LR
  Source["src/**/*.ts"] --> Typecheck["npm run typecheck"]
  Source --> Build["npm run build"]
  Source --> Lint["npm run lint"]
  Source --> Test["npm test"]
  Source --> SecretScan["npm run security:scan"]

  Build --> Dist["dist/"]
  Dist --> Start["npm start"]
  Dist --> CLIExec["node dist/cli/index.js"]
```

## 현재 검증 상태

| 항목 | 상태 |
| --- | --- |
| TypeScript typecheck | 통과 |
| Build | 통과 |
| Lint | 통과, warning 있음 |
| Test | 통과, 샌드박스 밖 실행 기준 |
| 기본 실행 | 통과 |
| CLI 주요 명령 | 통과 |

