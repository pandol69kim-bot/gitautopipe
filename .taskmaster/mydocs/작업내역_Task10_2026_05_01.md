# 작업내역 - Task 10: CLI 인터페이스 및 운영 대시보드 개발

**날짜**: 2026-05-01  
**브랜치**: main  
**커밋**: `447d976` (구현), `bfbfbe8` (서브태스크 상태 업데이트)

---

## 개요

셀피시 클럽 AI 에이전트 협업 시스템의 Task 10을 완료했습니다.  
Commander.js 기반 `selfish-club` CLI 도구를 구현했습니다.  
6개 핵심 명령어, Inquirer.js 인터랙티브 모드, 출력 포맷 옵션, 설정 파일 관리, 로깅 시스템을 포함합니다.

---

## 구현 파일

### 신규 생성

| 파일 | 역할 |
|------|------|
| `src/cli/logger.ts` | LogLevel 제어 Logger 클래스 (debug/info/warn/error) |
| `src/cli/formatter.ts` | 출력 포맷 변환 (table/json/minimal) |
| `src/cli/config-manager.ts` | selfish-club.config.json 로드/저장/검증 (Zod) |
| `src/cli/commands.ts` | 명령어 핸들러 (scan/sync/analyze/deploy/workflow/status) |
| `src/cli/interactive.ts` | Inquirer.js 인터랙티브 모드 (PromptFn 주입) |
| `src/cli/index.ts` | Commander.js 진입점 (`selfish-club` CLI) |
| `src/cli/cli.test.ts` | TDD 테스트 37개 |

### 패키지 추가

| 패키지 | 용도 |
|--------|------|
| `commander` | CLI 프레임워크 |
| `inquirer@8` | 인터랙티브 프롬프트 (CJS 호환) |
| `@types/inquirer@8` | Inquirer 타입 정의 |

---

## 구현 상세

### 1. CLI 진입점 (`src/cli/index.ts`)

```
selfish-club scan --folder mission       # 폴더 스캔
selfish-club sync --target github        # GitHub 동기화
selfish-club analyze --week 12           # 특정 주차 분석
selfish-club deploy --preview            # 프리뷰 배포
selfish-club workflow run weeklyDigest   # 워크플로우 실행
selfish-club status                      # 시스템 상태 확인
selfish-club interactive                 # 인터랙티브 모드
```

#### 글로벌 옵션

| 옵션 | 기본값 | 설명 |
|------|--------|------|
| `-o, --output <format>` | `table` | 출력 포맷 (table/json/minimal) |
| `--log-level <level>` | `info` | 로그 레벨 (debug/info/warn/error) |

### 2. Logger (`src/cli/logger.ts`)

```typescript
export type LogLevel = 'debug' | 'info' | 'warn' | 'error';

class Logger {
  constructor(level: LogLevel = 'info')
  setLevel(level: LogLevel): void
  getLevel(): LogLevel
  debug(message: string): void
  info(message: string): void
  warn(message: string): void
  error(message: string): void
}
```

- 레벨 순서: debug(0) < info(1) < warn(2) < error(3)
- 환경변수 `LOG_LEVEL` 또는 `--log-level` 옵션으로 제어
- 싱글톤 `logger` export로 전역 사용

### 3. Formatter (`src/cli/formatter.ts`)

| 포맷 | 출력 방식 |
|------|----------|
| `json` | `JSON.stringify(data, null, 2)` |
| `table` | 배열: 탭 구분 헤더+행 / 객체: key\tvalue |
| `minimal` | 배열: `count: N` / 객체: `key: value, ...` |

### 4. ConfigManager (`src/cli/config-manager.ts`)

- 설정 파일: `selfish-club.config.json` (프로젝트 루트)
- Zod 스키마 검증 (vault, github, notion, linkedin, logLevel)
- `load()`: 파일 없으면 기본 설정 반환
- `save(config)`: 검증 후 저장
- `init()`: 없으면 기본값으로 생성 후 반환
- 기본 설정:
  ```json
  {
    "vault": { "rootPath": "./vault", "folders": ["Mission", "Skills", "Insights", "Meetings"] },
    "github": { "owner": "", "repo": "", "branch": "main" },
    "logLevel": "info"
  }
  ```

### 5. Commands (`src/cli/commands.ts`)

| 함수 | 설명 |
|------|------|
| `runScan(opts, deps)` | 폴더 스캔 결과 반환 |
| `runSync(opts, deps)` | orchestrator.emit으로 동기화 이벤트 발행 |
| `runAnalyze(opts, deps)` | `onMissionUpdate` 워크플로우 실행 |
| `runDeploy(opts, deps)` | `skill:updated` 이벤트 발행 |
| `runWorkflow(opts, deps)` | 지정 워크플로우 직접 실행 |
| `runStatus(deps)` | 사전 정의 4개 워크플로우 상태 + 스케줄 수 반환 |

- `CommandDeps` 인터페이스로 orchestrator, outputFormat 주입 (테스트 가능성 확보)

### 6. Interactive (`src/cli/interactive.ts`)

- `PromptFn` 타입으로 inquirer 의존성 주입
- 메인 메뉴: scan / sync / analyze / deploy / workflow / status / exit
- 각 명령어별 추가 옵션 단계별 질문
- `InteractiveResult` 유니온 타입으로 타입 안전 반환

---

## TDD 결과

### 테스트 커버리지

```
Test Files: 14 passed (14)
Tests:      275 passed (275)   ← 신규 37개 포함
```

### 테스트 케이스 목록 (37개)

#### Logger (7개)
- 기본 레벨은 info이다
- 생성자로 레벨을 설정할 수 있다
- setLevel로 레벨을 변경할 수 있다
- 현재 레벨보다 낮은 레벨 메시지는 출력되지 않는다
- 현재 레벨 이상의 메시지는 출력된다
- error 레벨은 항상 출력된다 (error 레벨 설정 시)
- debug 레벨 설정 시 모든 레벨이 출력된다

#### Formatter (8개)
- 객체를 JSON 문자열로 변환한다
- 배열을 JSON 문자열로 변환한다
- 배열 데이터를 탭 구분 형태로 출력한다
- 빈 배열은 (no data)를 반환한다
- 객체 데이터를 key-value 형태로 출력한다
- 배열은 count만 반환한다
- 객체는 key: value 쌍을 반환한다
- 원시값은 문자열로 반환한다

#### ConfigManager (8개)
- 설정 파일이 없으면 기본 설정을 반환한다
- 설정을 저장하면 파일이 생성된다
- 저장 후 다시 로드하면 동일한 값이다
- exists()는 설정 파일 존재 여부를 반환한다
- init()은 파일이 없으면 기본 설정으로 생성한다
- init()은 파일이 이미 있으면 기존 설정을 반환한다
- getConfigPath()는 설정 파일 경로를 반환한다
- 잘못된 스키마의 설정 파일은 에러를 던진다

#### Commands (10개)
- runScan: 스캔 결과를 반환한다
- runScan: folder 미지정 시 all로 처리된다
- runSync: 동기화 결과를 반환한다
- runSync: target 미지정 시 github으로 처리된다
- runAnalyze: 분석 결과를 반환한다
- runDeploy: 배포 결과를 반환한다
- runWorkflow: 존재하는 워크플로우를 실행한다
- runWorkflow: 존재하지 않는 워크플로우는 에러를 던진다
- runStatus: 워크플로우 상태를 반환한다
- runStatus: 사전 정의 워크플로우 4개가 포함된다

#### Interactive (4개)
- scan 선택 시 folder 옵션과 함께 scan 명령어를 반환한다
- status 선택 시 status 명령어를 반환한다
- workflow 선택 시 workflowId 옵션과 함께 반환한다
- exit 선택 시 exit 명령어를 반환한다

---

## 서브태스크 완료 현황

| ID | 서브태스크 | 상태 |
|----|-----------|------|
| 1 | Commander.js 기반 CLI 프레임워크 설정 | ✅ done |
| 2 | 핵심 명령어 구현 (scan/sync/analyze/deploy/workflow/status) | ✅ done |
| 3 | 인터랙티브 모드 (Inquirer.js) 개발 | ✅ done |
| 4 | 출력 포맷 (table/json/minimal) 구현 | ✅ done |
| 5 | 설정 파일 (selfish-club.config.json) 관리 모듈 개발 | ✅ done |
| 6 | 로깅 시스템 및 도움말 (--help) 구현 | ✅ done |

---

## 커밋 정보

```
447d976 feat: Implement CLI interface and operation dashboard (Task 10)
bfbfbe8 chore: Update Task 10 subtask statuses to done
```

---

## Task 진행 현황

| ID | 태스크 | 상태 |
|----|--------|------|
| 1 | 프로젝트 초기 설정 및 개발 환경 구성 | ✅ done |
| 2 | Obsidian 볼트 구조 및 폴더 스캐너 구현 | ✅ done |
| 3 | GitHub 연동 및 코드 자동화 모듈 구현 | ✅ done |
| 4 | Claude Code 분석 엔진 구현 | ✅ done |
| 5 | Analysis 결과 자동 커밋 워크플로우 구현 | ✅ done |
| 6 | LinkedIn 게시물 자동 생성 시스템 구현 | ✅ done |
| 7 | Notion MCP 연동 및 Meetings 자동화 | ✅ done |
| 8 | Skill/Insight 게시물 자동 생성 시스템 (Vercel) | ✅ done |
| 9 | 자동화 워크플로우 오케스트레이터 구현 | ✅ done |
| **10** | **CLI 인터페이스 및 운영 대시보드 개발** | **✅ done** |
| 11 | AA 스타터 키트 템플릿 및 문서화 | ⏳ pending |
| 12 | 보안 및 권한 관리 시스템 구현 | ⏳ pending |
