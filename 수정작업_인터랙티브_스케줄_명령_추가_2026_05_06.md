# 수정작업_인터랙티브_스케줄_명령_추가_2026_05_06

작성일: 2026-05-06

## 수정 목적

기존 `interactive` 모드에서는 `schedule` 명령을 선택할 수 없어서,
스케줄 등록/조회/해제/즉시 실행/상주 실행을 모두 일반 CLI 인자 방식으로만 써야 했습니다.

이번 수정으로 인터랙티브 모드 안에서도 `schedule` 명령과 하위 작업을 직접 선택해 실행할 수 있도록 연결했습니다.

## 수정 파일

| 파일 | 내용 |
| --- | --- |
| `src/cli/interactive.ts` | 인터랙티브 명령 선택지에 `schedule` 추가, 하위 작업(`list`, `add`, `remove`, `run-due`, `start`) 프롬프트 및 반환 타입 추가 |
| `src/cli/index.ts` | 인터랙티브 모드에서 선택한 `schedule` 작업을 실제 CLI 실행 함수로 분기 연결 |
| `src/cli/cli.test.ts` | 인터랙티브 `schedule list`, `schedule add` 선택 경로 테스트 추가 |
| `프로젝트 명령어 정리.md` | 인터랙티브 모드에서 `schedule` 사용 가능함을 문서에 반영 |

## 동작 흐름

```text
node ./dist/cli/index.js interactive
	-> 명령 선택 화면 표시
	-> schedule 선택
	-> 하위 작업 선택
		 - list
		 - add
		 - remove
		 - run-due
		 - start
	-> 필요한 추가 입력 수집
	-> 기존 runSchedule* 함수 호출
	-> 결과 출력
```

## 인터랙티브에서 지원되는 schedule 하위 작업

| 작업 | 추가 입력 |
| --- | --- |
| `list` | 없음 |
| `add` | `workflowId`, `cron` |
| `remove` | `workflowId` |
| `run-due` | 선택 입력 `at` (ISO 시각) |
| `start` | 선택 입력 `intervalSeconds` |

## 사용 예시

```bash
npx ts-node src/cli/index.ts interactive
```

인터랙티브 메뉴에서:

```text
schedule
	-> add
	-> workflowId: onNotionSync
	-> cron: 0 9 * * *
```

## 검증 상태

- `npx vitest run src/cli/cli.test.ts -t "schedule .*선택"` 통과
- `npm run typecheck` 통과

## 참고 사항

- 전체 `src/cli/cli.test.ts` 실행 시 `runScan`, `runSync` 관련 기존 환경 의존 실패는 여전히 남아 있습니다.
- 이번 수정은 해당 기존 실패를 건드리지 않고, 인터랙티브 `schedule` 경로만 추가 및 검증했습니다.
