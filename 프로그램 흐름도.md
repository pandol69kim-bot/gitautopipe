# 프로그램 흐름도

작성일: 2026-05-01  
프로젝트 경로: `D:\TM_PROJECT_셀피시\코드엑스개발`

이 문서의 Mermaid 블록은 MD preview 호환성을 위해 `graph TD` / `graph LR` 문법만 사용합니다.

## 기본 실행 흐름

```mermaid
graph TD
User[User]
NPM[npm-start]
Main[dist-index-js]
Env[load-env]
Message[startup-message]
User --> NPM
NPM --> Main
Main --> Env
Env --> Message
Message --> User
```

현재 기본 실행은 서버를 계속 띄우는 방식이 아니라, 환경 변수를 로드하고 시작 메시지를 출력한 뒤 종료되는 구조입니다.

## CLI 실행 흐름

```mermaid
graph TD
Start[cli-command]
Dotenv[load-env]
Parse[parse-command]
PreAction[pre-action-hook]
Config[load-config]
Logger[set-log-level]
Secured[execute-cli-command]
Resource[build-resource]
Secrets[get-required-secrets]
Security[execute-secured-command]
Access[check-access]
RateLimit[check-rate-limit]
SecretCheck[check-secrets]
Handler[run-command-handler]
Format[format-result]
AuditSuccess[audit-success]
Output[print-result]
AuditFail[audit-failure]
Error[exit-with-error]
Start --> Dotenv
Dotenv --> Parse
Parse --> PreAction
PreAction --> Config
PreAction --> Logger
Config --> Secured
Logger --> Secured
Secured --> Resource
Secured --> Secrets
Resource --> Security
Secrets --> Security
Security --> Access
Access --> RateLimit
RateLimit --> SecretCheck
SecretCheck --> Handler
Handler --> Format
Format --> AuditSuccess
AuditSuccess --> Output
Access -.-> AuditFail
RateLimit -.-> AuditFail
SecretCheck -.-> AuditFail
Handler -.-> AuditFail
AuditFail --> Error
```

## CLI 명령별 흐름

```mermaid
graph LR
CLI[CLI]
Scan[scan]
Sync[sync]
Analyze[analyze]
Deploy[deploy]
Workflow[workflow-id]
Status[status]
Interactive[interactive]
ScanOut[scan-result]
SyncEvent[sync-event]
Mission[mission-workflow]
DeployEvent[deploy-event]
ById[workflow-by-id]
StatusOut[status-result]
Prompt[prompt-user]
Format[format-output]
CLI --> Scan
CLI --> Sync
CLI --> Analyze
CLI --> Deploy
CLI --> Workflow
CLI --> Status
CLI --> Interactive
Scan --> ScanOut
Sync --> SyncEvent
Analyze --> Mission
Deploy --> DeployEvent
Workflow --> ById
Status --> StatusOut
Interactive --> Prompt
Prompt --> CLI
ScanOut --> Format
SyncEvent --> Format
Mission --> Format
DeployEvent --> Format
ById --> Format
StatusOut --> Format
```

## 보안 처리 흐름

```mermaid
graph TD
CLI[CLI]
Sec[execute-secured-command]
Access[access-control]
Rate[rate-limiter]
Secret[secret-manager]
Handler[command-handler]
Audit[audit-logger]
Result[result]
CLI --> Sec
Sec --> Access
Access --> Rate
Rate --> Secret
Secret --> Handler
Handler --> Audit
Audit --> Result
Result --> CLI
```

## 워크플로우 등록 흐름

```mermaid
graph TD
New[new-orchestrator]
Register[register-predefined-workflows]
W1[onMissionUpdate]
W2[onMeetingSync]
W3[onSkillUpdate]
W4[weeklyDigest]
EventMap[event-listeners]
Schedule[schedule]
New --> Register
Register --> W1
Register --> W2
Register --> W3
Register --> W4
W1 --> EventMap
W2 --> EventMap
W3 --> EventMap
W4 --> Schedule
```

## 워크플로우 실행 흐름

```mermaid
graph TD
Execute[execute-workflow]
Find[find-workflow]
Context[create-context]
Loop[run-steps]
Step[execute-step]
Success[completed]
Failure[failed]
Policy[error-strategy]
Continue[continue-or-retry]
Stop[stop]
Next[next-step]
Check[all-steps-done]
Result[create-result]
History[save-history]
Return[return-result]
Execute --> Find
Find --> Context
Context --> Loop
Loop --> Step
Step --> Success
Step --> Failure
Failure --> Policy
Policy --> Continue
Policy --> Stop
Success --> Next
Continue --> Next
Stop --> Result
Next --> Check
Check --> Loop
Check --> Result
Result --> History
History --> Return
```

## 사전 정의 워크플로우

```mermaid
graph TD
M1[mission-log]
M2[claude-analyze]
M3[linkedin-draft]
N1[notion-fetch]
N2[obsidian-sync]
N3[report-update]
S1[site-build]
S2[vercel-deploy]
S3[deploy-notify]
W1[vault-scan]
W2[weekly-report]
W3[github-commit]
W4[digest-notify]
M1 --> M2
M2 --> M3
N1 --> N2
N2 --> N3
S1 --> S2
S2 --> S3
W1 --> W2
W2 --> W3
W3 --> W4
```

## 실제 검증 흐름

```mermaid
graph LR
Install[npm-install]
Typecheck[npm-run-typecheck]
Build[npm-run-build]
Start[npm-start]
CLIStatus[cli-status]
CLIScan[cli-scan]
CLIAnalyze[cli-analyze]
CLISync[cli-sync]
CLIDeploy[cli-deploy]
Weekly[workflow-weeklyDigest]
Lint[npm-run-lint]
Test[npm-test]
SecretScan[npm-run-security-scan]
Install --> Typecheck
Typecheck --> Build
Build --> Start
Build --> CLIStatus
Build --> CLIScan
Build --> CLIAnalyze
Build --> CLISync
Build --> CLIDeploy
Build --> Weekly
Typecheck --> Lint
Typecheck --> Test
Typecheck --> SecretScan
```

## 현재 실행 명령 예시

```powershell
npm run build
npm start
node dist\cli\index.js status --output json
node dist\cli\index.js scan --output json
node dist\cli\index.js analyze --week 1 --output json
node dist\cli\index.js sync --target github --output json
node dist\cli\index.js deploy --preview --output json
node dist\cli\index.js workflow weeklyDigest --output json
```

## 해석 주의

CLI와 워크플로우는 현재 로컬 실행 흐름 기준으로 정상 동작합니다. 다만 일부 워크플로우 step은 실제 외부 API 호출이 아니라 완료 메시지를 반환하는 형태입니다. 실제 GitHub, Claude, Notion, Vercel 라이브 연동 검증은 integration 모듈별 API 스모크 테스트를 별도로 실행해야 합니다.

