# 작업내용 - 2026-05-11

## 개요
`node ./dist/cli/index.js deploy` 명령이 실제 `WebsiteDeployer`를 통해 동작하도록 연결하고, 배포 완료 후 실제 URL 접속 확인까지 자동화했다.

## 주요 수정 내용

### 1. deploy 명령 실제 배포 연결
- `src/cli/commands.ts`
- 기존의 로그성 workflow emit 방식 대신 `WebsiteDeployer`를 직접 생성해 다음 순서로 실행하도록 변경
  - 소스 폴더 결정
  - 정적 사이트 빌드
  - Vercel 배포 생성
  - 배포 상태 폴링
  - 배포 URL 접근 검증
  - 완료 시 webhook 알림 전송
- 결과 출력에 실제 배포 정보 포함
  - `deploymentId`
  - `state`
  - `status`
  - `url`
  - `previewUrl`
  - `verificationStatus`
  - `verificationUrl`
  - `verificationHttpStatus`
  - `verifiedAt`

### 2. WebsiteDeployer 배포 로직 보강
- `src/workflows/website-deployer.ts`
- `buildSite()`가 `.build` 하위에 실제 산출물 생성
  - `index.html`
  - `search-index.json`
  - `{slug}.html`
- `deployToVercel()`가 로컬 경로 문자열을 보내지 않고, `.build` 폴더 파일을 `files` 배열로 업로드하도록 수정
- Vercel 에러 메시지를 중첩 객체에서도 읽을 수 있도록 개선
- `skipAutoDetectionConfirmation=1` query parameter 추가

### 3. 배포 상태 대기 자동화
- `waitForDeploymentReady()` 추가
- Vercel 상태를 READY, ERROR, CANCELED까지 폴링하도록 구현
- 관련 환경 변수 추가
  - `DEPLOY_STATUS_MAX_ATTEMPTS`
  - `DEPLOY_STATUS_POLL_INTERVAL_MS`

### 4. 배포 URL 실제 접속 확인 자동화
- `verifyDeploymentUrl()` 추가
- READY 상태 후 실제 배포 URL에 GET 요청 수행
- 검증 결과를 다음으로 분류
  - `verified`: 2xx 응답
  - `protected`: 401 또는 403 응답
  - `unreachable`: 반복 시도 후 실패
- 관련 환경 변수 추가
  - `DEPLOY_VERIFY_MAX_ATTEMPTS`
  - `DEPLOY_VERIFY_INTERVAL_MS`

### 5. protected 상태 처리
- Vercel 배포 URL이 401/403을 반환하는 경우를 오류로 보지 않고 `protected` 상태로 처리
- 이 경우 배포 자체는 성공으로 유지하고, 접근 보호 상태만 결과에 표시

### 6. 보안 및 예시 환경 변수 정리
- `src/cli/security.ts`
- `deploy` 명령 필수 시크릿을 다음으로 정리
  - `VERCEL_TOKEN`
  - `VERCEL_PROJECT_ID`
- `.env.example`에 배포 관련 선택 환경 변수 추가

## 테스트 및 검증

### 테스트
다음 테스트를 중심으로 검증 완료
- `src/cli/cli.test.ts`
- `src/workflows/website-deployer.test.ts`
- `src/cli/security.test.ts`

검증 결과
- 관련 테스트 88개 통과
- `npm run build` 성공

### 실제 명령 검증
실제 실행
```bash
npm run build
node ./dist/cli/index.js deploy
```

최종 확인 결과
- 배포 생성 성공
- 상태 `READY`
- URL 검증 수행됨
- 현재 배포 URL은 공개 응답이 아니라 보호 상태로 판별됨
  - `verificationStatus: protected`
  - `verificationHttpStatus: 401`

## 실제 관찰된 이슈와 조치

### 이슈 1. Vercel 400 + `[object Object]`
원인
- 잘못된 payload 형식
- 에러 객체를 문자열로 부정확하게 출력

조치
- `files` 배열 업로드 방식으로 수정
- 에러 message 추출 로직 추가

### 이슈 2. `INITIALIZING` 상태를 실패로 처리
원인
- 진행 중 상태를 실패로 분류

조치
- `INITIALIZING`을 진행 중 상태로 분류하고 예외 미발생 처리

### 이슈 3. 배포 URL 401
원인
- 배포는 완료되었지만 URL 접근이 보호됨

조치
- `protected` 상태 도입
- 배포 실패가 아니라 보호 상태로 보고

## 산출 문서
- `기능 작동 분석서.md` 작성 및 정리
- 오늘 작업 요약 문서 생성

## 비고
- 현재 `deploy`는 dry-run 없이 실제 Vercel deployment를 생성한다.
- webhook 실패는 여전히 전체 명령 실패로 전파된다.
- 배포 URL이 `protected`로 나오는 것은 배포 실패가 아니라 접근 정책 상태다.
