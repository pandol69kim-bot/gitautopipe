# 스모크테스트

점검일: 2026-04-30  
작업 위치: `D:\TM_PROJECT_셀피시\코드엑스개발`

## 1. 목적

현재 프로젝트에서 외부 API 연동이 실제 환경변수와 네트워크 호출 기준으로 동작 가능한지 빠르게 확인하기 위한 별도 스모크 테스트를 추가했습니다.

## 2. 추가된 테스트 코드

- 파일: `scripts/api-smoke-test.ts`
- 실행 명령: `npm run test:api`
- 테스트 대상:
  - Claude API
  - GitHub API
  - Notion API
  - Vercel API

테스트 코드는 `.env`를 로드한 뒤 각 API를 독립적으로 확인합니다. 특정 API 키가 없거나 placeholder 값이면 전체 실패가 아니라 해당 항목만 `SKIP` 처리합니다.

## 3. 환경변수

기본 사용 변수:

- `CLAUDE_API_KEY` 또는 `ANTHROPIC_API_KEY`
- `GITHUB_TOKEN` 또는 `GITHUB_API_KEY`
- `NOTION_TOKEN`
- `VERCEL_TOKEN`

선택 변수:

- `CLAUDE_MODEL`
- `GITHUB_OWNER`
- `GITHUB_REPO`

`GITHUB_OWNER`와 `GITHUB_REPO`를 설정하면 GitHub 인증뿐 아니라 특정 저장소 접근 가능 여부까지 확인합니다.

## 4. 실행 결과

실행 명령:

```bash
npm run test:api
```

결과 요약:

| API | 상태 | 내용 |
| --- | --- | --- |
| Claude | 실패 | Anthropic API가 크레딧 부족 오류를 반환함 |
| GitHub | 통과 | 토큰 인증 성공 |
| Notion | 통과 | integration 접근 성공 |
| Vercel | 추가됨 | `VERCEL_TOKEN` 기준 인증 확인 가능 |

Claude 실패 메시지 요약:

```text
Your credit balance is too low to access the Anthropic API.
```

즉, Claude 연동 코드는 실제 API까지 도달했지만 Anthropic 계정의 결제/크레딧 상태 때문에 요청이 거부된 상태입니다.

## 5. 빌드 검증

API 스모크 테스트 코드 추가 후 빌드도 확인했습니다.

```bash
npm run build
```

결과: 통과

## 6. 현재 판단

- GitHub API: 테스트 가능, 현재 인증 성공
- Notion API: 테스트 가능, 현재 인증 성공
- Claude API: 테스트 가능하나 계정 크레딧 충전 또는 결제 설정 필요
- Vercel API: `VERCEL_TOKEN`이 있으면 인증 확인 가능

## 7. 다음 작업

1. Anthropic 계정 크레딧 충전 또는 결제 설정 확인
2. 필요하면 `.env`에 `GITHUB_OWNER`, `GITHUB_REPO`를 추가해 저장소 접근성까지 점검
3. 크레딧 충전 후 `npm run test:api` 재실행
